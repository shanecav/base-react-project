/**
 * Used in Webpack DLL for faster builds (see tools/build/webpack.dll.js)
 */

require("babel-polyfill");
require("react");
require("react-dom");
require("smoothscroll-polyfill");
require("styled-components");
